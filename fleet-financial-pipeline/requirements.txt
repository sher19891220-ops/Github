# Parsing the sources
pdfplumber>=0.10        # BofA statement PDFs
openpyxl>=3.1           # every .xlsx: P&L, settlements, fuel and toll rails
pandas>=2.0

# Querying and reporting
duckdb>=1.0             # SQL over the corpus without standing up a database
XlsxWriter>=3.2         # formatted workbooks back to the operator
matplotlib>=3.8         # charts for reports
rarfile>=4.0            # upload archives (needs the 7z binary, already present)

# Database target
psycopg2-binary>=2.9

# Tests
pytest>=8.0

# Pulling the weekly P&L straight from Google Sheets (ingest/pull_sheets.py).
# Optional: everything else works from the .xlsx already on disk, so a container
# without a service account key is fully functional and simply cannot refresh.
google-api-python-client
google-auth

# QuickManage's own API (ingest/pull_quickmanage.py). Optional in the same way.
requests>=2.31

# Reading answers aloud (tools/say.py, .claude/skills/voice). Optional -- the
# pipeline is fully functional without it. The 63 MB voice model lives in
# /opt/piper-voices, outside the repo; say.py names the install command if the
# container was reclaimed and took it.
piper-tts
