{
  "token": "samsara_api_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
  "_note": "Copy to samsara_credentials.json (gitignored, matches config/*_credentials.json). Or set SAMSARA_API_TOKEN to this same token in the environment instead -- preferred, since it survives a container reclaim without a file to re-upload. One token covers the whole group (ZONE/XTRACK/AFG) -- there is no per-company pair the way QuickManage needs."
}
