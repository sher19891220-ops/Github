{
  "api_key": "iak_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
  "staging_api_key": "iak_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
  "_note": "Copy to relay_payments_credentials.json (gitignored, matches config/*_credentials.json). Or set RELAY_PAYMENTS_API_KEY / RELAY_PAYMENTS_STAGING_API_KEY to these same values in the environment instead -- preferred, since it survives a container reclaim without a file to re-upload. The staging key is lower-risk to test with (Relay's sandbox, not real fuel data). See ingest/pull_relay_fuel.py's module docstring: the API's actual base URL and transaction response shape are UNCONFIRMED as of 2026-09-23 -- docs.relaypayments.com requires a login this pipeline intentionally never stores. Run `python3 ingest/pull_relay_fuel.py --whoami` once a key is set to test it."
}
