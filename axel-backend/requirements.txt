fastapi>=0.115.0
uvicorn[standard]>=0.32.0
anthropic>=0.34.0
python-dotenv>=1.0.0
httpx>=0.27.0
beautifulsoup4>=4.12.0
duckduckgo-search>=6.0.0
docker>=7.0.0
apscheduler>=3.10.0
aiofiles>=24.0.0
pydantic>=2.0.0

# Google Workspace (Gmail, Sheets, Calendar, Drive)
google-api-python-client>=2.140.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0

# Bank accounts (Plaid — uses REST API via httpx, no SDK needed)
# plaid-python is optional — install if you need the SDK

# QuickBooks
python-quickbooks>=0.9.0
intuit-oauth>=1.2.0

# Microsoft / Outlook
msal>=1.30.0
requests>=2.32.0
