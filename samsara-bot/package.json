{
  "name": "samsara-telegram-bot",
  "version": "1.0.0",
  "private": true,
  "description": "Samsara fleet alerts, PTI/DVIR compliance checks and on-demand lookups, delivered to Telegram. Runs as Vercel serverless functions.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "vercel dev"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@vercel/node": "^5.0.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "engines": {
    "node": ">=20"
  }
}
