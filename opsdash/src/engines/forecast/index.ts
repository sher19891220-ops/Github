export { calculateForecast, ForecastInputError, WINDOW_WEEKS, MIN_BACKTEST_POINTS } from './calculate';
export type {
  BackTestPoint,
  ForecastMetric,
  ForecastResult,
  MetricForecast,
  WeekActual,
} from './types';
