# npm 10's peer resolver crashes on vitest 4's optional @vitest/browser-* peer
# set ("Cannot read properties of null (reading 'edgesOut')"). Without this,
# a clean `npm install` fails outright — including inside every worktree a
# subagent creates, where the failure would look like a broken checkout.
legacy-peer-deps=true
