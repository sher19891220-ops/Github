/** The code the test server is configured with. Never a real one. */
export const ACCESS_CODE = 'test-access-code-8412';

/** Where the signed-in session is cached between projects. */
export const STORAGE_STATE = 'test-results/.auth/session.json';
